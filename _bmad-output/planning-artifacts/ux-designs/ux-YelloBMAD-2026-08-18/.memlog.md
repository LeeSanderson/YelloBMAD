---
topic: Yello UX — DESIGN.md + EXPERIENCE.md spines
updated: 2026-08-20T15:24
---

- (decision) Sources confirmed by Lee: PRD (prd-YelloBMAD-2026-08-15/prd.md + addendum.md) and architecture spine (architecture-YelloBMAD-2026-08-17/ARCHITECTURE-SPINE.md). Spec kernel (_bmad-output/specs/spec-yello/) and brainstorming outputs deliberately NOT ingested. Flagged to Lee: spec kernel is newer and contains surfaces-and-journeys.md; risk of surface-name drift against downstream epics/stories. Open.
- (decision) Working mode: Coaching path. Creative tools woven into Discovery.
- (event) Inherited by reference from PRD: IA surfaces (PRD 7, eight surfaces), journeys UJ-1..UJ-8 (protagonists Ravi, Nadia, Beatriz, Tomas), glossary (PRD 2, verbatim - no synonyms), Role capability matrix (FR-16), accessibility floor (NFR-9, WCAG 2.1 AA on registration/Space switching/Board/Task editor/invitation), scale envelope (NFR-8). From architecture: Blazor WebAssembly .NET 10 on Azure Static Web Apps; AD-11 client edits a local replica and never blocks on network; AD-15 fractional-index board ordering; AD-24 only Space switcher and Account settings are Account-scoped; ARIA live regions for Presence and permission-change notices.
- (override) Earlier spec-drift flag corrected. The PRD absorbed the spec kernel's six resolutions on 2026-08-18 (PRD section 11), so the PRD is current and carries FR-42, NFR-2's two clauses, FR-27 report-and-ask and FR-41 bulk. It is the ARCHITECTURE SPINE that is stale: scope reads 'FR-1...FR-41', AD-5 still describes an immediate ownership transfer with no pending state, and the ER diagram has no OwnershipOffer entity. Consequence for UX: where PRD and spine disagree on the ownership path, the PRD governs. Verified by reading both. Not ingesting the spec kernel remains fine.
- (event) SURFACE CLOSURE FAILURE found (open, needs Lee). FR-8 permits an Ownership Offer to name a Membership of ANY Role, and its assumption places the offer 'in Space settings rather than emailed'. But PRD section 7 scopes Space settings to Owner and Admin only. So a Member or Viewer named in an Ownership Offer has no surface on which to see, accept or decline it - and UJ-8 is exactly that case: Nadia is a Member when she accepts. FR-42 requires only the named Membership can accept, so the affordance must reach a non-admin Role. Do not invent the surface; probe Lee.
- (event) UX run PAUSED at Lee's direction before Discovery brain dump. Reason: architecture spine is stale against the 2026-08-18 PRD revision and building UX on it would push the defect downstream. Resolving via bmad-architecture Update mode, scope Gaps 1+2+3: (1) AD-8 line 122 cites NFR-2's retired 5-second bound; NFR-2 is now two clauses, no-tolerance on the request path and 1 second on the live-session path. (2) FR-42 Ownership Offer has no architecture: no OWNERSHIP_OFFER entity, scope reads FR-1...FR-41, AD-5 still says 'ownership transfer is one transaction', fourth deletion cascade (lapse-on-Membership-removal) unlisted, FR-42 atomicity against the filtered unique Owner index unstated, AD-9 dual-Membership publish unstated. (3) 7-day expiry unmechanised for both FR-39 Invitation and the Offer against AD-10's no-timer rule. NOT stale, do not touch: FR-27 (AD-16+AD-17 cover report-and-ask), FR-41 bulk (AD-17+AD-18), NFR-8 bounds (AD-25; PRD confirmed rather than revised). Resume UX at the Discovery brain dump once the spine is re-finalized.
- (decision) Standing rule for this UX run: where the PRD and the architecture spine disagree on the ownership path, the PRD governs. Revisit once the spine is updated.
- (event) ARCHITECTURE BLOCKER RESOLVED 2026-08-19. Spine updated to 28 ADs, lint clean, scope now FR-1...FR-42. The standing rule logged earlier (PRD governs over the spine on the ownership path) is now RETIRED — the spine is current and the two agree. Directly relevant to UX: (1) AD-26 authorises accepting an Ownership Offer by ROW IDENTITY, not Role, and FR-8 permits any Role to be named, so the mechanism will let a Viewer accept and deliberately does not Role-gate it — the surface must therefore reach any Role. (2) AD-26 further fixes that the named recipient reads a pending offer INSIDE the Space own context under AD-2, and explicitly rules out a cross-Space offers inbox because that would breach AD-24 (only the Space switcher and Account settings are Account-scoped). So the UX answer is constrained: whatever surface renders the pending offer must sit inside Space context, not in a global inbox or notification centre. The remaining open question for UX is narrower than before — which in-Space surface, given PRD section 7 scopes Space settings to Owner and Admin only. Still a PRD section 7 defect as much as a UX one. (3) AD-28 added: presenting an Invitation is a side-effect-free read and acceptance is a separate explicit act — so the invitation-acceptance flow needs a real confirmation step in the UI, not a link that lands and joins. That shapes UJ-3 (Nadia).
- (event) UX run RESUMED 2026-08-19 by Lee. Architecture blocker cleared. Working mode reconfirmed: Coaching path. Stakes set: CONSUMER - so the accessibility lens is a dedicated reviewer at Finalize and NFR-9's WCAG 2.1 AA floor is a hard gate, not a nice-to-have.
- (event) Sources re-read in full post-spine-update (prd.md 867 lines, ARCHITECTURE-SPINE.md 417 lines, 28 ADs). Form-factor RESOLVED BY SOURCE, no probe needed: PRD 9.2 'the web interface is responsive; there is no native client' + Blazor WASM on Static Web Apps. So: responsive web, single codebase, no native surface. Additional UX-bearing constraints picked up that the earlier inheritance entry did not name: (a) AD-3 the 403/404 line - a Space-boundary miss renders as not-found, a within-Space Role refusal renders as permission-denied, and copy must never let a user tell which; (b) FR-16 'the interface hiding an action is never the mechanism that enforces it' - absent affordances are a UX contract, not a security one; (c) FR-28 at the NFR-8 bound of 5000 Tasks the Board must still satisfy NFR-5 AND NFR-9 simultaneously, and the PRD states plainly the three cannot all hold naively - paging/virtualisation is architecture's call but it lands on Board UX; (d) AD-26 a refused offer transition returns 409 with a stable problem type, never 404 - so there is a real 'this offer is no longer pending' state to design; (e) Consistency Conventions row 'Accessibility' repeats NFR-9's five flows and mandates ARIA live regions for Presence and permission-change notices; (f) SM-C2 - UJ-1's success condition is Ravi CLOSING THE TAB, time-in-app is an explicit counter-metric, so engagement patterns are anti-goals.
- (decision) Lee's brain dump, verbatim intent: 'The obvious comparison is Trello. The idea for this project comes directly from the capabilities of that product and the usability is key.' So Trello is the primary reference product and the origin of the capability set, and usability is named as the top-priority quality. Captured as-is; which LAYER of Trello (capability shape / interaction feel / visual look) is being pointed at is being probed rather than assumed, because the three pull in different directions and one of them collides with PRD non-goals.
- (decision) Trello reference scope: ALL THREE LAYERS confirmed by Lee - capability shape, interaction feel, AND visual look. So Trello is not merely a functional benchmark; its visual language (card surfaces, column chrome, rounded density) is an admitted influence on DESIGN.md. Interaction feel is already substrate-supported by AD-11 (client edits a local replica, never blocks on network) so optimistic instant-landing edits are architecturally free rather than aspirational. Consequence: colour-theme and design-direction creative tools are in scope and Lee picks from rendered options.
- (decision) Usability bar: Lee selected ALL FOUR priority flows - the Board (FR-28/29), Space switching + Role legibility (UJ-4), first 90 seconds (UJ-1), and collaborative editing (UJ-5/UJ-6). This exactly matches NFR-9's five accessible flows (registration, Space switching, Board, Task editor, invitation) minus invitation, so the accessibility floor and the usability priorities are near-coextensive - one set of key screens serves both. Consequence for Finalize: Board, Space switcher/context bar, registration-to-first-Task, and Task detail with Presence all get key-screen mocks. Invitation acceptance added to that list since NFR-9 covers it and AD-28 gives it a real confirmation step to design.
- (decision) DELETION RESOLVED: irreversible stands, UX carries the weight. Lee declined to reopen the FR-7/FR-17 assumption despite the PRD flagging it for review and despite the Trello-archive muscle-memory collision. No trash, no restore, no split treatment between Task and Space. Consequence for EXPERIENCE.md: destructive-action confirmation is a named State Pattern with friction scaled to blast radius (Task < Project < Space < Account), and Voice and Tone must make irreversibility explicit at the point of action per PRD 6.2 'this is stated at the point of the action'. This is a UX-owned mitigation of an accepted product risk - record it as such so a later reader does not read the friction as arbitrary.
- (decision) Open concerns ALL FOUR adopted by Lee: dark mode, motion, content density, internationalisation. Consequences: (1) dark mode as a peer theme means every DESIGN.md colour token carries paired light/dark values from the outset, not a derived dark palette; (2) motion becomes a specified DESIGN.md concern with named durations/easings since Trello interaction feel is the target and unspecified motion means each developer invents timing; (3) content density needs an explicit position and possibly a comfortable/compact control, driven by FR-28's 5000-Task bound; (4) i18n adopted DESPITE no upstream demand and English being the configured document_output_language - so EXPERIENCE.md needs externalised copy, text-expansion-tolerant layout and RTL-tolerant structure. Flagged at selection time that i18n is a real cost with no stated requirement behind it; Lee took it anyway.
- (decision) Board touch path: LONG-PRESS TO LIFT THEN DRAG. Lee chose the Trello-faithful option after being told plainly it is the most fragile of the four - it competes with page scroll, misfires on scroll intent, and is the interaction most likely to feel broken on mid-range Android. Accepted as a deliberate fidelity-over-robustness trade. Consequences EXPERIENCE.md must carry: the Board now has THREE genuinely distinct input paths (pointer drag, long-press touch drag, keyboard) and long-press does NOT discharge NFR-9 - the keyboard path is still separately mandated and must be designed, not derived from the gesture. Interaction Primitives must specify long-press threshold, scroll-intent disambiguation, haptic/visual lift feedback, and auto-scroll at column edges. Risk logged rather than resolved: if this tests badly on touch, tap-to-select-then-place is the fallback and it is cheaper because it collapses toward the keyboard path.
- (decision) Name is NOT load-bearing on the palette. Lee: 'It's a pun - don't lean on it.' So 'Yello' is the greeting reading (or simply a liked name), not a colour instruction, and the palette is fully open with no obligation to hue-match the name and no yellow-forward requirement. Recorded explicitly because a later reader WILL assume the product is yellow from the name alone and that assumption is wrong. Corollary: DESIGN.md Brand & Style must not derive voice from the colour pun either.
- (decision) No imports. Lee has no prior sketches, Figma, palette or brand assets - starting clean, everything in DESIGN.md originates in this session. imports/ stays empty and there is no reconcile-{slug}.md work owed at Finalize for user-supplied visuals. Only the sources (PRD, architecture spine) are inherited by reference.
- (event) Creative tool run: colour themes round 1 rendered to .working/color-themes-1.html. Five directions differing in REGISTER not hue - A Studio (Trello-adjacent, cool blue, cards floating on tinted column ground), B Slate (near-zero chroma, saturated colour reserved for meaning only), C Paper (warm neutral grounds, ink text, deep green-teal accent), D Signal (border-defined not shadow-defined, highest contrast, density-tolerant), E Dusk (dark-first blue-violet, light theme as the adaptation). Every panel carries the same nine semantic roles, each justified by a Yello requirement rather than decoration: page/column/card (FR-28 Board grounds), text/muted (NFR-9 AA), accent, focus (kept SEPARATE from accent because NFR-9 makes the ring load-bearing), presence (FR-32, rendered with a text count alongside the dot so it is never colour-alone), role (UJ-4 permanent chrome), danger (irreversible deletion per 6.2). Each snippet deliberately exercises the three states the palette must survive - focused, lifted-mid-drag with drop zone, and destructive-confirm. Awaiting Lee's pick.
- (decision) COLOUR DIRECTION CHOSEN: E - Dusk. Deep blue-violet grounds, DARK-FIRST with light as the derived adaptation. Confirmed hex, dark (primary): page #0B1020, column #121A31, card #18213C, border #2A3552, text #E6EAF5, muted #9AA6C4, accent #8B80F9, accent-on #14103A, focus #ADA4FB, presence #38BDF8, danger #FB7185, role #241F52 on #C3BCFC. Light (adaptation): page #F4F6FB, column #EAEEF7, card #FFFFFF, border #D7DEEC, text #131A2B, muted #4B5670, accent #5145CD, accent-on #FFFFFF, focus #6E62E6, presence #0E7490, danger #BE123C, role #E2E0FA on #332B96. Consequences that follow from picking the dark-first option specifically: (1) DESIGN.md Colors must present dark as the canonical column and light as the derived one, NOT the usual light-primary ordering - otherwise implementers will build light and invert it, which is exactly what this choice rejects; (2) the accepted trade-off flagged at selection time stands - a strongly tinted ground competes with user-defined Label colours (FR-22) for attention, so the Label colour-range constraint in Components is now REQUIRED rather than nice-to-have; (3) E uses shadow-based elevation, so the density risk that direction D solved with borders is unresolved and must be handled in Layout/Elevation at the FR-28 5000-Task bound.
- (event) Creative tool run: design directions round 1 rendered to .working/directions-1.html. Palette held FIXED at Dusk dark; the four directions vary only the still-open axes (density, type and weight, shape and elevation, implied motion timing). Hero screen is the Board for all four, plus two constraining states each - the same Board as a Viewer (UJ-4 affordances absent not dimmed) and an empty Project (UJ-1 must not ask to configure). Directions: 1 Compact (12.5px/500 titles, 6px radius, ~2x Task density, reads as control panel); 2 Roomy (15px/450, 14px radius, half the density, best empty state, worst UJ-4 legibility); 3 Editorial (16px serif titles, rule separators instead of boxes, no shadow - sidesteps Dusk's weak shadow separation on tinted ground but a rule-separated row invites a click not a lift, which fights the chosen long-press gesture); 4 Engineered (3px radius, brighter borders #33406B, zero shadow, ALL metadata monospace, strongest Role legibility hence strongest on UJ-4, risks reading cold to Beatriz the client Viewer). Light adaptation rendered for directions 1 and 4 (the density/shape extremes) to confirm the tinted-blue light ground survives both ends before committing it. Awaiting Lee's pick.
- (decision) DESIGN DIRECTION CHOSEN: 4 - Engineered, on the Dusk palette. Confirmed spec: radius 3px (rSm 2px), card padding 9/10, gap 6, Task title 13px weight 600 sans, column head 10.5px weight 700 uppercase letter-spacing .12em, ALL metadata in monospace (counts, Labels, avatar initials, Role chip), borders lifted to #33406B, ZERO shadow - structure is drawn not floated, avatars square-ish (3px radius), implied motion budget 90-120ms near-instant. Consequences: (1) the Dusk-tinted-ground elevation risk is now RESOLVED by construction rather than mitigated - dropping shadow entirely removes the weak-separation problem and makes the design density-tolerant for FR-28's 5000-Task bound; (2) strongest of the four on UJ-4 Role legibility, which was one of Lee's four named usability priorities, because the monospace Role chip reads as a system fact rather than a decorative badge; (3) ACCEPTED RISK, flagged at selection and taken anyway - the engineered register may read cold to Beatriz, the paying client Viewer who never asked for a developer tool. Mitigation deferred to Voice and Tone rather than visual warmth, since the visual decision is now fixed; (4) 90-120ms near-instant motion is coherent with AD-11's local-replica-first editing, so the interaction feel Lee asked for from Trello is architecturally free. Note: the empty-state copy shown in the mocks ('Nothing in Admin yet / Add a Task and get on with it') was explicitly labelled placeholder to Lee, NOT an elicited voice decision - voice remains unelicited.
- (decision) SURFACE CLOSURE FAILURE RESOLVED. The pending Ownership Offer is surfaced on the SPACE CONTEXT BAR. Rationale accepted by Lee: the context bar already exists for every Role, is already Space-scoped under AD-2, and already answers 'what is my standing here' - an Ownership Offer is precisely a proposed change to that standing, so it is the semantically correct home rather than a convenient one. Requires NO amendment to PRD section 7 (the bar is not Role-scoped) and NO amendment to AD-24 (it is Space-scoped, not Account-scoped). Two-part design consequence for EXPERIENCE.md: the context bar carries the INDICATOR only; the DECISION opens on a surface with room to explain what accepting commits the recipient to - per FR-42 the new Owner's Membership cannot then be removed while they hold ownership and their Account deletion is refused until they transfer onward or delete the Space, which is a genuinely consequential commitment and cannot be conveyed in a chrome-sized affordance. Also needs the AD-26 409 state - offer no longer pending (accepted, declined, revoked, lapsed, or lost to a concurrent offer) - as a real designed state, not an error toast.
- (override) UPSTREAM PRD AMENDMENT OWED - decided here, must be carried back. Lee chose to EMAIL the Ownership Offer, reversing the 'rather than emailed' half of PRD assumption #3 (section 4.2 FR-8). Trap this closes, found during UX constraint analysis and not previously recorded anywhere: (1) FR-8 surfaces the offer in-Space and explicitly not by email; (2) it expires after 7 days; (3) AD-27 computes expiry on read with no timer; (4) section 4.11 has NO notification for ownership - FR-39 covers Invitations, FR-40 covers Task assignment, nothing covers this. Composed, the named recipient is never told and must happen to enter the Space within 7 days or the offer lapses silently. UJ-8's own edge case then bites: 'if Nadia declines, or lets the offer lapse, Ravi is still the Owner and still cannot leave.' Combined with FR-3 refusing Account deletion while he owns a Space, Ravi's only unilateral exit is destroying the Space and everyone's work in it. So Yello shipped a path where an Owner is trapped indefinitely because someone never logged in. Stark asymmetry named: being assigned a Task emails you (FR-40); being offered an entire Space does not. Cost of the fix is low and was verified before recommending - assumption #3 is NOT marked dagger so it has not hardened into architecture, the outbox and email adapter already exist for FR-39, AD-27's ExpiresAt handling is unchanged, and sending at the transaction boundary leaves AD-10's no-timer rule intact. ACTION FOR bmad-prd: amend FR-8 to remove 'rather than emailed', and add an ownership-offer notification to section 4.11 as a new FR alongside FR-39/FR-40. ACTION FOR bmad-architecture: none believed necessary - confirm the outbox covers it. Until the PRD catches up, downstream epics will not include the email, so this must be surfaced at handoff.
- (decision) UJ-9 ADDED, closing the last surface-closure gap - protagonist Ravi, trigger 'retiring a Project safely'. Numbered structure: (1) Ravi has finished the discovery phase of Northwind and wants the Project 'Discovery' gone, but its Tasks are worth keeping - he knows FR-17 deletion destroys them irreversibly, so he does not start with delete. (2) He opens PROJECT SETTINGS on Discovery to read its effective Status set, because the count of Statuses is the count of operations this will take - this is the step that gives Project settings its first journey. (3) The effective set is the Space defaults Todo/In Progress/Done plus a delta that added 'Parked' - four Statuses, so at most four moves. (4) From the Board's Done column he runs the FR-41 bulk move to 'Website Rebuild'; Done exists in the destination's effective set so the Status is preserved and he is asked for no mapping. (5) He repeats from Todo and In Progress - same, no mapping. (6) 'Parked' does not exist in the destination's effective set, so this move requires a destination Status supplied as part of the operation and he chooses Todo. (7) Discovery is now empty and he deletes it. CLIMAX: the irreversible operation he was afraid of became safe because he emptied the Project first, and the confirmation can tell him it holds zero Tasks - the friction and the reassurance are the same mechanism. RESOLUTION: one fewer Project, no work lost, and he never reached for an undo because there isn't one. EDGE CASES: a bulk move that cannot complete is refused rather than partially applied and the refusal is visible not silent (FR-41); if Nadia is mid-edit on a Task description in the moved selection her collaborative session continues across the move and she is not disconnected (FR-41); and honestly - nothing stops Ravi deleting Discovery with Tasks still in it, FR-17 permits it and destroys them, so the safe path is a choice the UX must make attractive rather than a guardrail the product enforces.
- (event) NEW FINDING surfaced while structuring UJ-9, not previously recorded in PRD or spine. FR-41's bulk form is ATOMIC and UNBOUNDED in size - 'every selected Task moves, or none does' - while NFR-8 permits 5,000 Tasks per Project, so a single bulk move can be a 5,000-row atomic transaction. Against that: NFR-5 wants writes inside 500ms p95, and the deployment is Azure SQL serverless free offer at min 0.5 vCore with MAXDOP=1 forced by AD-2 to dodge the SESSION_CONTEXT parallel-plan defect. AD-17 fixes that the move is one transaction but bounds nothing about its size; AD-25 enforces NFR-8 as creation refusals but says nothing about operation size. So whether a 5,000-row atomic reparent-plus-Status-migration lands inside NFR-5 on 0.5 vCore serial execution is unaddressed. UX CONSEQUENCE regardless of how architecture answers it: the bulk move needs a real in-flight/pending state with the operation's scope stated ('moving 4,812 Tasks'), because FR-41 requires the refusal to be 'visible rather than silent' and a multi-second atomic write with no pending state reads as a hung interface. EXPERIENCE.md State Patterns must carry it. Flag to architecture rather than resolve here - it is a mechanism question, and the honest options are bounding the bulk size as an AD-25 refusal or accepting that this one operation is exempt from NFR-5.
- (decision) VOICE: terse and factual. Short declaratives, no hedging, no apology, no exclamation marks. 'Access ended.' 'This cannot be undone.' Treats the reader as competent and matches the Engineered visual register rather than softening it. REFUSAL COPY: flat and unexplained - 'Not available.' plus a route back to a Space the Account does belong to. Leaks nothing, never risks a phrasing that hints across the Space boundary, and satisfies FR-15/AD-3 by saying so little that gone and not-yours are genuinely indistinguishable. COMPOUNDING RISK, named once and accepted rather than re-litigated: Lee has now chosen the colder option at three consecutive forks - Engineered visuals over Roomy/Paper, terse voice over plain-but-human, and flat refusal over naming-the-ambiguity. That is a coherent and defensible product position (it suits Ravi the freelance developer and Tomas the API user, and it honours SM-C2 where a fast exit beats a warm session) but it concentrates all the risk on ONE persona: Beatriz, the paying client who is a Viewer, never asked for a developer tool, and meets Yello only through a link someone sent her. Recorded so that if Beatriz's experience is ever reported as a problem, the cause is documented and the three decisions that produced it can be revisited together rather than one at a time. NOT flagged as a defect - it is a deliberate, thrice-confirmed direction.
- (event) SPINES DISTILLED. DESIGN.md written with full literal token values (no UI system inherited, so no delta pattern applies) - dark tokens unsuffixed as canonical with -light suffix for the adaptation, plus a product-specific 'motion' token group and a Motion section appended AFTER the canonical eight body sections so the locked order is preserved rather than reordered. EXPERIENCE.md written with the eight required sections, both triggered sections (Inspiration & Anti-patterns - Trello named at all three layers; Responsive & Platform - three breakpoints), and FOUR invented sections that earn their place: 'Space Context and Role Legibility' (the product's central bet, absent-not-disabled, no standard section owns it), 'Isolation and Refusal' (the AD-3 403/404 line is a copy problem with a security consequence that neither Voice nor State Patterns can hold alone), 'Status Configuration' (FR-27's report-and-ask cascade is the most complex interaction in the product), and 'Internationalisation' (adopted with no upstream requirement, so the obligations need stating or they will not be met). Rubric Pass 1 run proactively before offering the gate.
- (change) Rubric Pass 1 self-run found and FIXED five mechanical gaps before the Reviewer Gate. (1) Flow coverage: UJ-2 and UJ-7 had been marked 'inherited, no UX delta' rather than written - both now have full numbered flows with climax and failure paths, so all nine journeys are covered. (2) Component coverage was broken in BOTH directions - EXPERIENCE.md named nine components with no DESIGN.md visual spec (space-switcher, task-detail, description-editor, picker, bulk-move-bar, invitation-view, destructive-confirm, plus the inert-editor state) and DESIGN.md named three with no behavioural row (role-chip, column-count, avatar). All now specified on both sides. (3) State coverage was missing five real states traceable to specific requirements: Task deleted while open (FR-23 requires participants be TOLD, not silently dropped), sign-in failure uniformity including response DURATION per AD-23, API Token displayed exactly once (FR-36), List View filter empty, and NFR-8 bound refusal via AD-25. All added. (4) Contrast targets were absent - now stated as a verified table for every load-bearing dark combination. (5) Visual reference coverage remains legitimately empty; mockups/ and wireframes/ do not exist until the key-screens step.
- (change) CONTRAST FAILURE RESOLVED - Lee chose to raise the border to clear 3:1, accepting the visual cost. border-hairline #33406B -> #6478A8 (dark), border-hairline-light #D7DEEC -> #7A88A8 (light). Verified: dark border now 3.4:1 on surface-card, 3.9:1 on surface-column, 4.3:1 on surface-page; light border 3.6:1 on white card, 3.1:1 on surface-column-light. All clear WCAG 1.4.11's 3:1 non-text minimum. Root cause recorded in DESIGN.md so nobody softens it back: with elevation removed the card-to-column tonal step is only 1.1:1, effectively invisible, so the border carries component identity ALONE - at the originally drawn #33406B it was 1.7:1 and the pair failed together, and the light theme failed identically at 1.35:1, which proved the problem was hairline-on-near-tone rather than anything specific to the dark values. ACCEPTED CONSEQUENCE: the surface now reads more GRIDDED than direction 4 was first drawn, because borders are genuinely present rather than suggested. That is the true cost of combining a tinted close-toned palette with no-shadow structure, and Lee elected to pay it in the design rather than at the accessibility audit. A Do-and-Don't row and an explicit 'do not lower this token' now guard it. NOTE: the two .working/ artifacts still show the ORIGINAL #33406B - they are historical records of the decision process and are deliberately not retro-edited; key-screen mocks rendered from here on use the corrected value.
- (event) REVIEWER GATE RAN - three lenses, all three returned, reports at review-rubric.md, review-accessibility.md, review-isolation.md. 111 findings total, 8 critical. Rubric: overall disciplined first draft, flow coverage and shape fit STRONG, but component and state coverage THIN and three things block clean source-extraction. Accessibility: NOT GATE-READY - 6 critical including a WCAG 2.5.1 Level A failure (no single-pointer non-path alternative to the Board drag on a touch tablet at 768-1279px), the arrow+Space keyboard grammar being inoperable in NVDA/JAWS browse mode, and no specified focus destination for the FR-34 interruption which inerts or deletes the very node holding focus. Isolation: NO SPECIFIED LEAK and the switcher/offer-indicator discipline verified consistent across four independent statements, but 19 findings all in the 'under-specified so an implementer could easily create a leak' category - the spines specify the server-facing half of isolation and are near-silent on the client-facing half. Most serious: AD-11's client replica has no specified lifetime, so a Blazor WASM client plausibly keeps Space A's Board and unsynchronised Task text resident across a Space switch and a lease invalidation, and re-renders it optimistically before the 404 lands - a render SM-1's request-level isolation suite cannot see.
- (change) REVIEWER FINDINGS RESOLVED - all 8 criticals plus the high-severity set. (1) WCAG 2.5.1 Level A failure closed by adding a CANONICAL non-gesture Move control on every Task, available at every breakpoint - this single fix also resolves the browse-mode critical, because a standard control and menu work in NVDA/JAWS where the arrow+Space grammar cannot. Restructured Interaction Primitives so the explicit control is the conformance path and all three gestures are ACCELERATORS layered on top; long-press is retained exactly as Lee chose it, since 2.5.1 requires an alternative rather than a replacement. (2) Focus destinations specified for all seven remote/dynamic events, including the FR-34 case where the inerted node HELD focus (goes to the assertive banner, made programmatically focusable) and the virtualised-row-recycling case (focus follows Task IDENTITY, never the row element - otherwise Space picks up the wrong Task, a data-corrupting bug reachable only by keyboard). (3) Contrast table recomputed from hex with the real WCAG formula - 8 of my 12 figures were wrong, all in the safe direction, but my 'little headroom, do not darken' warning was on accent at 4.96 when the actual thinnest margin is border-hairline-light on surface-column-light at 3.06. Both themes now stated in full. (4) TWO genuine AA failures found in pairs the table never stated: accent-as-text-link against body copy at 2.66 dark / 2.55 light, below 1.4.1's 3:1, now fixed by mandating an underline and adding a text-link component; and role-chip fill against the context bar at 1.05, which made the chip invisible as a container, now fixed with a 1px border - its TEXT was always fine at 8.56, so this was a failure to read as a chip rather than a contrast failure, and it mattered because UJ-4 turns on Role legibility. (5) Live-region storms mitigated - Presence coalesces on a trailing window and announces state not events, never interrupts active typing; permission changes are never throttled since there is at most one and it has earned the interruption; the Board never announces loading or virtualising. (6) Duplicate Component Patterns rows for Task detail and Description editor removed (my error), pickers consolidated. (7) Space settings and Project settings given real behaviour - Membership list, Invitation list, Status delta editor, Ownership panel - closing the gap where FR-12/13/14 had nothing to build against despite FR-14 being UJ-6's trigger. (8) Two missing states added: removed-while-disconnected (the composition neither existing row covered - the lease invalidation CANNOT reach her offline, so she edits on believing she has access and learns only on reconnect; resolves to 'Access ended.' not to a reconciliation failure, because her text was never admissible rather than lost to a fault) and session expired. (9) Theme resolution mechanism stated - the -light suffix is resolved once at the theme boundary and components consume semantic names only, so the 11 apparently-unreferenced -light tokens are the mechanism working rather than a gap.
- (change) CLIENT-SIDE ISOLATION added as a new requirement block in EXPERIENCE.md, closing the isolation lens's most serious finding. AD-2 enforces Space scoping in the DATABASE and AD-11 has the client editing a LOCAL REPLICA, which leaves a gap SM-1's suite structurally cannot see: its isolation cases exercise requests, and a leak rendered from client memory issues no request. Six behavioural rules now bind the client - discard all Space-scoped state on Space switch (explicitly a DATA rule, not a visual one, covering Board contents, Task text, unsynchronised edits, Assignee/Label/Status lists, filters, scroll position and any id-keyed cache); the same discard on AD-9 lease invalidation; full discard on sign-out or Session expiry; NO Space-scoped data in localStorage/sessionStorage/IndexedDB, which extends AD-7's credential ban to content and also stops browser session-restore repopulating a Space after access ended; optimistic rendering never precedes authorisation on FIRST read of a Space, since a remembered Board rendered while the 404 is in flight is a short-lived disclosure rather than a fast interface; and an id in a URL confers nothing. Deliberate asymmetry recorded: on lease invalidation her own unsynchronised TEXT stays visible (she typed it, it is already on her screen, per FR-34) while everything that came FROM the Space is discarded.
- (event) TWO FINDINGS ESCALATED to bmad-architecture rather than resolved in UX, both recorded in EXPERIENCE.md at the relevant section. (A) TIMING ORACLE on the 403/404 line. I had asserted the two 404 cases are indistinguishable including 'same latency'. That is not deliverable from the interface: AD-20 has the pipeline write an AccessRefusal row for every SPACE-BOUNDARY 404, while a genuinely non-existent resource crosses no boundary and so plausibly writes nothing - different work, different duration, a timing oracle no amount of identical copy can close. AD-23 already makes duration part of the contract for registration and authentication; the same discipline is owed here and no AD states it. Claim in EXPERIENCE.md softened to 'every respect the interface controls' with the gap named explicitly. (B) THE FR-28 COLLISION IS UNOWNED. FR-28 requires the Board to satisfy NFR-5 and NFR-9 simultaneously at 5,000 Tasks and the PRD says the three cannot all hold naively, then defers mechanism to the architecture; the 28 ADs never choose paging, virtualisation or windowing (AD-13 and AD-15 fix persistence and ordering, AD-25 only enforces the bound as a creation refusal); and this spine deferred back. A three-way deferral means nobody owns it. Flagged with the note that whoever decides it must decide focus and announcement behaviour in the SAME pass, because virtualisation is precisely what makes focus-follows-identity and true-total counts hard, and both are NFR-9 release-gate obligations.
- (change) SECOND FIX PASS - both spines rewritten to absorb the remaining ~50 findings the three reports raised beyond the first pass. Note the reviews read the files BEFORE the first fix pass landed, so a number of their findings (duplicate rows, Session expiry, removed-while-disconnected, contrast recompute, role-chip border, text-link underline, light-theme resolution, Membership/Invitation lists) were already closed; those were verified rather than re-fixed. DESIGN.md changes: border-hairline-light raised #7A88A8 -> #6B7794 because 3.06 was a 2% margin on a release gate and the light tonal ladder is FLATTER than the dark one (1.07 vs 1.10), so the light border matters more not less; new borders token group with hairline-width 1.5px because a 1px border antialiases at the 1.25x/1.5x/1.75x display scales a 3px grid produces and every border pair drops under 3:1 at 80% coverage, and task-card-lifted's rotate(-1deg) antialiases UNCONDITIONALLY so the one object whose boundary matters most was guaranteed to fail - it now takes a 2px emphasis border; whole type scale moved to rem with every line-height raised to >=1.5, because absolute px ignores a user font-size preference (1.4.4) and lineHeight 1 cannot absorb the 1.5x override 1.4.12 permits; role-label and meta raised from 9px/10px to 0.6875rem; new presence-count token set in SANS not mono, since the Presence string is prose rather than a system fact AND is the mandated non-colour carrier of Presence under NFR-9 - the smallest text in the product should not be what the gate rests on; new revoked-edge token separating the FR-34 editor edge from danger, which resolved a real self-contradiction where Do's-and-Don'ts forbade danger on ordinary refusals while the component used it for one; inert -> readonly throughout, because inert removes content from the accessibility tree and would have made the retained text unreachable, exactly defeating the reason it is retained; foreground tokens added to the six components that had none; the focus-ring rationale corrected - focus-ring vs accent is 1.45:1 so it WOULD vanish, and what actually protects it is the 2px outline-offset putting it on the ground behind, so the rule is now geometric; accent-vs-danger stated at 1.19:1 and the destructive signal reassigned to COPY with the border as reinforcement, since the two converge under deuteranopia; Label constraint now requires 3:1 against BOTH card grounds (the same user-chosen colour renders in both themes, 17 stops apart), names DeltaE2000 explicitly, and adds accent and presence to the exclusion set; Task card metadata row given a real overflow rule (max three Label chips then +N, wrap before scroll) because Labels are uncapped and 10 concurrent editors turn '2 editing' into '10 editing' inside 282px at the reflow width; target floor stated as 24px per WCAG 2.2 AA 2.5.8 rather than the previously asserted 44px, which is a AAA criterion the components contradicted - self-consistency restored deliberately rather than by weakening; density-at-200%-zoom recorded as an accepted cost so the FR-28 argument is not read as holding at every zoom level.
- (change) EXPERIENCE.md second pass. ISOLATION additions: multi-tab rule - a permission notice goes only to the client context whose active Space matches the change, cross-tab fan-out is a per-tab FILTER never a shared announcement, and the copy stays 'Access ended.' with no Space name, because an implementer broadcasting to every tab then needs to disambiguate which Space ended and the string becomes 'Access to Northwind Redesign ended.' spoken in a tab showing a different Space, with screen-reader users taking the disclosure first; inbound-frame purge ORDERED BEFORE the banner plus clearing both live regions, closing the case where a queued Presence or remote-edit frame renders one tick after access ended; email-address rule stated for the first time (section 6.1 confines addresses to Owners and Admins of a Space the Account is a Member of - EXPERIENCE.md had never mentioned it, and the field every implementer reaches for to disambiguate two identical display names IS the address) plus a Membership-scoped discriminator requirement and attribution-by-captured-name rather than live global lookup; browser-owned surfaces - autocomplete off on every Space/Project/Task name field and above all the delete-Space confirmation (origin-scoped autofill would offer the next Account another Account's Space name), fixed document.title of 'Yello', no Space name in the Invitation view's title or og metadata plus noindex (a pasted link otherwise scrapes the Space name into the chat logs of people with no Membership), and scroll restoration disabled on Space-scoped routes; the three Account-scoped reads richer than AD-24's letter each decided explicitly rather than left to a slice - owned-Space names and the Spaces-per-Account count PERMITTED as own-data with AD-24 amendments flagged, and the API Token Role display DROPPED as a genuine Membership read across a boundary that was decoration rather than function; the Ownership Offer EMAIL given the same disclosure clause the Invitation email has, since it is the one new outbound artefact, NFR-1 binds notifications, and no FR governs it yet; boundary-404-on-optimistic-write given a terminal state so the implementer is not forced to choose between a silent revert and a forbidden toast; cold load corrected - the context bar may render its shell but NEVER the Space name, which could only come from cache and would both distinguish removed-from from never-existed and render one Account's Space name to another on a shared profile; and the rule that the client never issues a Space-scoped request for a Space id absent from the current switcher response, which closes warm-start and deep-link-after-removal together and stops the client manufacturing CrossSpace refusals that would bury real probing in its own noise (NFR-7). ACCESSIBILITY additions: Space scoped to Task cards only so it keeps native activation and paging elsewhere (Board columns ARE scroll containers); arrow keys bound to LOGICAL direction so they mirror under RTL exactly as the layout does, resolving a direct contradiction between two sections; sticky-origin-index-with-clamping named for cross-column traversal; the keyboard drop destination specified as same-ordinal-clamped, which AD-15 needs since it computes a fractional index between two concrete neighbours; the live-region announcement string shape specified ('Moved to In Progress, position 3 of 12') because for a blind user that sentence REPLACES the entire visual drop-zone system; Esc precedence stated as innermost-wins; the sub-768px keyboard path routed through the Move picker so a carried Task cannot enter an off-screen column; aria-setsize/aria-posinset required so the true-total chip and the accessibility tree agree; Board remote-mutation announcement policy DECIDED (no per-Task announcement, debounced summary, manual refresh) rather than left open; skeletons aria-busy; Space-switch focus and polite announcement stated explicitly rather than inherited from the generic dialog rule, since it is a gated flow and a changed accessible name does not reliably re-announce; the Status pager specified as a tablist because it is the sole navigation route at the 1.4.10 test width; destructive confirms replace their panel in place rather than stacking two deep; a submit-in-flight state for the only submit-and-wait interactions in the product; and the role-capability clause in the context bar's accessible name, which is the compensating mechanism that makes absent-not-disabled INTERPRETABLE rather than merely true - the review confirmed absence is defensible and needs one clause added, not reversing. COVERAGE additions: FR-6 rename-a-Space given a home (an inherited PRD section 7 omission, flagged); Membership inserted into the destructive blast-radius ladder with a live-session warning, since FR-14 is UJ-6's trigger and was missing; 'Role drops below a surface's requirement' added as the general case of FR-34; List View at-scale and filter/sort controls added for FR-30, which explicitly requires NFR-5 and NFR-9 at 5000 on the same terms as FR-28; Status-migration in-flight and refused states added, since FR-27's transaction is strictly larger than the bulk move that already had them; disconnection on non-editor surfaces; the Invitation terminal-state uniformity row covering unrecognised and forged tokens alongside revoked; the wrong-Account-signed-in row that refuses WITHOUT echoing either address; UJ-2 and UJ-7 written as full flows; a 403 copy exemplar added and the Voice 'Don't' row qualified, resolving a contradiction where the only permission-refusal string in the document sat in the banned column while another section required exactly that kind of statement; refusal copy made capability-shaped rather than object-shaped; 'held locally' softened to 'not yet sent' so the copy stops promising application; glossary discipline - User capitalised, the banned synonym 'workspace' removed, the full section 2 term list declared, and the UJ-4 climax quotation restored to include the repeated ', absent' that is the rhetorical point of the original.
- (event) SYNTHESIS PIPELINE RUN. validation-report.html rendered from the skeleton template with the eight rubric categories as collapsible dimensions plus a section per extra lens, and validation-report.md written as the Markdown twin grouped by severity. One deliberate extension to the template: a fixed/open/escalated status chip per finding, added because the three reviewer reports were written BEFORE the fix passes and therefore cannot carry the fixed-versus-open distinction themselves - without it the consolidated report would read as 111 outstanding problems rather than 106 resolved ones. Final disposition: 106 resolved, 2 escalated to bmad-architecture (the 403/404 timing oracle and the unowned FR-28 x NFR-5 x NFR-9 collision), 3 open by explicit decision (Internationalisation kept standalone against the rubric's fold recommendation; spellcheck/IME egress accepted and flagged at section 6.4's gate; the .working exploration artifacts left showing superseded borders as an honest record of the decision process).
- (event) MOCK COVERAGE CONFIRMED and closed. Walked all nine IA surfaces; Lee elected to mock three of the five that were spine-only. Now mocked (7): Board, Ownership Offer, Task detail, Registration/Space home, Project settings, Space settings, Invitation view. Deliberately SPINE-ONLY (2), logged as a considered choice rather than an omission: the List View (a filterable table whose paging, keyboard traversal and at-scale contract are fully stated, and whose layout does not drive behaviour) and Account settings (a settings list whose one interesting state, an API Token displayed exactly once, is likewise stated). The three new mocks target the highest-risk prose: project-settings renders FR-27's report-and-ask cascade with both halves - the rename-collision single cascade decision across all conflicting Projects, and the removal table naming every Project that cannot accept the Space-wide destination WITH its affected Task count, unset pickers reading 'Choose...' rather than a pre-filled value, and the commit disabled while STATING WHY - because section 11 records that FR-27's original defect was one half guessing while the other asked, and a default selection would reintroduce the guess. space-settings renders the same data as Owner and as Admin so that all four differences read as ABSENCES (no Role picker on the Owner's row, own Role shown as fact not control since Admins cannot modify each other, no Ownership panel and no delete button at all rather than greyed ones, and Role pickers offering Member/Viewer only) plus the only surface in the product where email addresses appear, a live-session warning on the row of someone mid-edit, and autocomplete=off on the Space name field. invitation renders the side-effect-free read, the five-terminal-states-one-response refusal including an unrecognised token, the wrong-Account case naming NEITHER address, and the in-flight state that matters most because AD-23 makes this deliberately the slowest interaction in the product. All seven mocks promoted to mockups/ and linked inline from both spines with spines-win-on-conflict stated.
- (event) POLISH COMPLETE, doc_standards applied in configured order. STRUCTURAL review: measured rather than assumed - across 165 DESIGN.md and 521 EXPERIENCE.md sentences of 8+ words, exactly ONE appears verbatim in both files (the intentional spine-wins header note), so the two fix passes did not layer duplicate text and the earlier prefers-reduced-motion cross-file duplication is resolved. Ten recommendations, five applied: (1) the FR-34 cluster lifted out of the State Patterns table into a subsection, because 38 rows with eight cells over 79 words - longest 103 - had stopped being a table, and the rules that matter most about that cluster are ORDERING rules a grid cell cannot carry; (2) Browser-owned surfaces converted from five prose bullets to a mechanism/rule table; (3) a new 'Open against this spine' block in Foundation consolidating the three unresolved items, because the two escalations sat 271 lines apart in unrelated sections and a contract's unresolved items are its highest-value scan target and were its least findable; (4)(5) two DESIGN.md condenses removing restated reasoning adjacent to its own canonical statement. Four PRESERVE findings recorded so a later editor does not undo them - the 275-line frontmatter is the machine-readable half of a token contract not overhead; the three-place pattern for load-bearing colour rules (reasoning in Colors, application in Components, rule in Do's and Don'ts) is correct for random access even though it reads as repetition linearly; Internationalisation stays per Lee's decision; and the linear Status Configuration and Key Flows inserts are deliberate. Net change roughly -195 words on 16,835 - these documents are dense reference, not bloat. PROSE review: three definitive fixes - 'the viewer' was ambiguous against the Viewer Role, 'the project' collided with the Project glossary term where it meant the software effort, and the single soft 'should not imply' strengthened to 'must not' in a document otherwise carrying 25 must and 57 never. Two queries flagged and deliberately NOT changed with reasoning recorded (lowercase 'user-defined'/'user-chosen' as generic technical compounds; 'a Kanban board' as an external pattern name). Critically PRESERVED: 'Colors' and 'colors:' are spec-mandated by the Google Labs design.md contract and are NOT American-spelling errors - a naive British-spelling pass would break the token contract. Verified the split is exact: all 85 occurrences of 'color' are token paths or the spec heading, all 14 prose occurrences use 'colour'. No doubled words, no agreement errors, no unclear antecedents, zero standalone lowercase glossary terms in EXPERIENCE.md.
- (event) spines finalized. DESIGN.md and EXPERIENCE.md both status: final, updated 2026-08-20. Final size 17,159 words (DESIGN 5,107 / EXPERIENCE 12,052). Deliverables: 2 spines, 7 mockups, 3 reviewer reports, 1 consolidated validation report in HTML and Markdown, 2 exploration artifacts in .working, and a 40-entry decision record. Open at handoff and carried in the spines' own Foundation section: two items for bmad-architecture (the 403/404 timing oracle, the unowned FR-28 x NFR-5 x NFR-9 collision) and one for bmad-prd (email the Ownership Offer, amending FR-8's assumption and adding a section 4.11 notification). Five live [ASSUMPTION] tags, none blocking. workflow.on_complete and workflow.external_handoffs are both empty in customize.toml, so no automated handoff to execute.
